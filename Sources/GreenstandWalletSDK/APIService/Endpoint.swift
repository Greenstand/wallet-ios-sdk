import Foundation

enum Endpoint: String {
    case wallets = "wallet/wallets"
    case transfers = "wallet/transfers"
    case auth = "wallet/auth"
    case tokens = "wallet/tokens"
    case trees = "query/trees"
    case wallet = "query/wallets/"
}
